{
  "name": "kinectconnect",
  "version": "0.1.0",
  "private": true,
  "description": "KinectConnect tutorial workspace",
  "author": "Thomas J McLeish",
  "license": "MIT",
  "scripts": {
    "install:server": "npm --prefix server install --omit=optional",
    "install:server:full": "npm --prefix server install",
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start"
  },
  "engines": {
    "node": ">=18"
  }
}
